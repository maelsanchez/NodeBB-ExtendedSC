<div class="checkbox">
    <label><input type="checkbox" data-property="extendedsc:toggles:hide" <!-- IF settings.extendedsc:toggles:hide -->checked<!-- ENDIF settings.extendedsc:toggles:hide -->> Hide extendedsc</label>
</div>
<div class="checkbox">
    <label><input type="checkbox" data-property="extendedsc:toggles:sound" <!-- IF settings.extendedsc:toggles:sound -->checked<!-- ENDIF settings.extendedsc:toggles:sound -->> Play a sound on a new shout</label>
</div>
<div class="checkbox">
    <label><input type="checkbox" data-property="extendedsc:toggles:notification" <!-- IF settings.extendedsc:toggles:notification -->checked<!-- ENDIF settings.extendedsc:toggles:notification -->> Display a notification in the titlebar on a new shout</label>
</div>